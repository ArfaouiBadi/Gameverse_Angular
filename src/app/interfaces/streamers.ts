export interface Streamers {
    id?: string,
    name?: string,
    bio?: string,
    category?: string,
    verified?: boolean,
    folowers?: number,
    image?: string,
    age?: number,
    email?: string,
    
}
